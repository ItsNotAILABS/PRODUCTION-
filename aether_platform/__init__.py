"""
Aether Sovereign Platform — Python reference backend.

Fleet management, workload orchestration, ring-based policy, and the
protocol registry, in stdlib-only Python. Named `aether_platform` (not
`platform`) because `platform` is a reserved Python stdlib module name —
the collision made the package unimportable in every invocation style
(`python -m platform.api.server`, direct script, PYTHONPATH tricks).
"""
