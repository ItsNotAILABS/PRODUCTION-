"""
Aether Sovereign Platform — Python reference backend.

Fleet management, workload orchestration, ring-based policy, and the
protocol registry, in stdlib-only Python. Renamed from `platform/` to
`aether_platform/` because `platform` is a reserved Python stdlib module
name and the collision made the package unimportable in every invocation
style (`python -m platform.api.server`, direct script, PYTHONPATH).
"""
