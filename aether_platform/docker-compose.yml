services:
  aether-platform:
    build: .
    image: aether-platform:latest
    container_name: aether-platform
    restart: unless-stopped
    ports:
      - "7700:7700"
    environment:
      - PYTHONUNBUFFERED=1
