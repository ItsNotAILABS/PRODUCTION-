fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0

# Multi-language structure extraction. Optional at runtime: without it Python
# still parses via the stdlib and every other language degrades to metadata
# only, which the card says out loud rather than silently returning nothing.
tree_sitter>=0.25
tree_sitter_language_pack>=1.14
