{
  "name": "weekly-command-center-cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Containers deployment: Worker router + static assets + the single app container.",
  "scripts": {
    "sync-assets": "bash scripts/sync-assets.sh",
    "dev": "npm run sync-assets && wrangler dev",
    "deploy": "npm run sync-assets && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.107.0"
  }
}
