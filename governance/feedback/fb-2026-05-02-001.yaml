# Example feedback file — release override for visual flaky tests
# This is a seed example showing how meta engine learns from overrides.

id: "fb-2026-05-02-001"
actor: "human://operator"

target:
  type: "decision"
  entity: "atlas://bot/organism-release-bot"
  law_id: "governance://law/BOT_FLEET_SAFETY"
  rule_name: "NO_RELEASE_ON_RED"
  pipeline_id: "pipeline://governance/bot_cycle"
  run_id: "seed-example"

decision:
  system: "FORBID"
  human: "ALLOW"

context_snapshot:
  health_dashboard: "red"
  risk_score: 0.35
  findings:
    - "visual_flaky_test"
    - "visual_screenshot_mismatch"

rationale: "Red due to flaky visual tests only (non-deterministic screenshots). Core build, tests, and security all green. Safe to release."

law_proposal:
  type: "CONDITION_RELAXATION"
  suggestion: "Add exception: when findings only contain 'visual_*' patterns and risk_score < 0.5, allow release with warning rather than hard block."

at: "2026-05-02T18:30:00Z"
